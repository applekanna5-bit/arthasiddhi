import Link from "next/link";
import { ArticleCallout } from "./ArticleCallout";
import { ArticleFAQ } from "./ArticleFAQ";
import { Breadcrumbs } from "./Breadcrumbs";
import { RelatedArticles, RelatedCalculators } from "./RelatedContent";
import { TableOfContents } from "./TableOfContents";
import { categoryLabels } from "@/lib/content/articles";
import { getRelatedCalculators } from "@/lib/content/calculators";
import { getRelatedArticles } from "@/lib/content/articles";
import type { Article } from "@/lib/content/types";
import { SiteNav } from "@/components/site/SiteNav";

function formatDate(date: string) { return new Intl.DateTimeFormat("en-IN", { day: "numeric", month: "short", year: "numeric", timeZone: "UTC" }).format(new Date(`${date}T00:00:00Z`)); }

export function ArticleLayout({ article }: { article: Article }) {
  const category = categoryLabels[article.category];
  return <main className="min-h-screen bg-slate-50 px-4 py-6 text-slate-900 sm:px-6 sm:py-8 lg:px-8"><div className="mx-auto max-w-6xl"><header className="mb-8 flex flex-wrap items-center justify-between gap-4 border-b border-slate-200 pb-5"><Link href="/" className="text-sm font-semibold tracking-[0.18em] text-emerald-700 uppercase">ArthaSiddhi</Link><SiteNav /></header><article><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Learn", href: "/learn" }, { label: category, href: `/learn/${article.category}` }, { label: article.title }]} /><header className="mt-7 max-w-3xl"><Link href={`/learn/${article.category}`} className="text-sm font-semibold tracking-wide text-emerald-700 uppercase">{category}</Link><h1 className="mt-3 text-3xl font-bold tracking-tight text-slate-950 sm:text-5xl">{article.title}</h1><p className="mt-4 text-lg leading-8 text-slate-600">{article.description}</p><div className="mt-5 flex flex-wrap gap-x-4 gap-y-1 text-sm text-slate-600"><span>By {article.author}</span><span>Published {formatDate(article.publishedAt)}</span><span>Updated {formatDate(article.updatedAt)}</span><span>{article.readingTime}</span></div></header><div className="mt-10 grid gap-8 lg:grid-cols-[15rem_minmax(0,1fr)]"><aside className="lg:sticky lg:top-6 lg:self-start"><TableOfContents sections={article.sections} /></aside><div className="space-y-10"><div className="space-y-10">{article.sections.map((section) => <section key={section.id} id={section.id} className="scroll-mt-6"><h2 className="text-2xl font-bold tracking-tight text-slate-950">{section.heading}</h2>{section.paragraphs?.map((paragraph) => <p key={paragraph} className="mt-4 leading-7 text-slate-700">{paragraph}</p>)}{section.list && <ul className="mt-4 list-disc space-y-2 pl-5 leading-7 text-slate-700">{section.list.map((item) => <li key={item}>{item}</li>)}</ul>}{section.table && <div className="mt-5 overflow-x-auto rounded-xl border border-slate-200"><table className="min-w-full text-left text-sm"><caption className="border-b border-slate-200 bg-slate-50 px-4 py-3 text-left font-medium text-slate-700">{section.table.caption}</caption><thead className="bg-slate-50 text-slate-700"> <tr>{section.table.headers.map((header) => <th key={header} scope="col" className="px-4 py-3 font-semibold">{header}</th>)}</tr></thead><tbody className="divide-y divide-slate-200 bg-white text-slate-700">{section.table.rows.map((row) => <tr key={row.join("-")}>{row.map((cell) => <td key={cell} className="px-4 py-3 align-top leading-6">{cell}</td>)}</tr>)}</tbody></table></div>}{section.callout && <div className="mt-5"><ArticleCallout {...section.callout} /></div>}</section>)}</div>{article.faq && <ArticleFAQ items={article.faq} />}<RelatedCalculators calculators={getRelatedCalculators(article.relatedCalculators)} /><RelatedArticles articles={getRelatedArticles(article)} /><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Learn", href: "/learn" }, { label: category, href: `/learn/${article.category}` }, { label: article.title }]} /></div></div></article></div></main>;
}
